# Copy to `.env`, fill in the value, and keep `.env` local (it is gitignored).
# xskill.ai / 速推AI — keys: https://www.xskill.ai/#/v2/api-keys
# `mcp-config.json` references ${XSKILL_API_KEY}; run ./generate-mcp-config.sh to expand.
XSKILL_API_KEY=your_key_here
